services:
  builder:
    build:
      context: .
      dockerfile: docker/Dockerfile
    volumes:
      - ./src:/app/src
    #prevents the container from exiting immediately if there's a minor error
    stdin_open: true
    tty: true
